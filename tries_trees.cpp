#include <bits/stdc++.h>
using namespace std;
struct tries{
    vector<tries*> v;
    tries(): v(0,NULL){}
};
struct trees{
    trees *left,*right;
    trees(): left(NULL),right(NULL){}
};
int main(){

}